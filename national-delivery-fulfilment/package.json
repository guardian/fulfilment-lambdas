{
  "name": "national-delivery-fulfilment",
  "version": "1.0.0",
  "description": "National Delivery fulfilment",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@aws-sdk/client-s3": "^3.391.0",
    "@types/aws-lambda": "^8.10.119",
    "@types/node": "^20.5.0",
    "aws-lambda": "^1.0.7",
    "aws-sdk": "^2.1439.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  },
  "scripts": {
    "build": "yarn esbuild --bundle --platform=node --target=node18 --outfile=dist/index.js src/index.ts",
    "package": "cd dist; zip -qr ../national-delivery-fulfilment.zip ./*"
  }
}
